{
  "name": "israeli-budget-tracker",
  "version": "1.0.0",
  "description": "Personal monthly budget tracking application for Israeli residents",
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:coverage": "vitest --coverage",
    "build": "tsc && node build.js",
    "dev": "tsc && node build.js && echo 'Open public/index.html in your browser'"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.0.0",
    "fast-check": "^3.15.0",
    "jsdom": "^28.1.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
